{
  "name": "dova-futures-site",
  "version": "1.0.0",
  "private": true,
  "description": "Dova Futures Developers website with contact backend",
  "main": "worker.mjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build-static-assets.mjs",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "format": "prettier --write index.html worker.mjs worker.test.mjs scripts/build-static-assets.mjs site-audit.spec.js package.json playwright.config.js .github/workflows/ci.yml",
    "format:check": "prettier --check index.html worker.mjs worker.test.mjs scripts/build-static-assets.mjs site-audit.spec.js package.json playwright.config.js .github/workflows/ci.yml",
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "node --test worker.test.mjs",
    "test:e2e": "playwright test"
  },
  "dependencies": {},
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "prettier": "^3.9.6",
    "wrangler": "^4.38.0"
  }
}
