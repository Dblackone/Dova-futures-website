# DOVA FUTURES LIMITED logo pack

Synced from the shared company design system on 22 September 2026. The eight PNGs are the current placement variations; `logo-manifest.json` is the selection index. Preserve each image's aspect ratio and clear space.

## Website selections

- Dark navigation header and page footer: `02-horizontal-cream-on-green.png`.
- Browser favicon and Apple touch icon: `06-symbol-cream-on-green.png`.
- For other placements, select from `logo-manifest.json` and the placement table in `logo-usage.md`.

These are opaque RGB PNGs, not transparent overlays. The square light stacked version is marked review-only because of a faint line above the right tower. A vector master for the new variations is still pending; use the references under `references/` only where the existing template lockup and services descriptor are required. They are legacy template-compatible SVGs, not vector masters of the new pack.

The prior assets in the parent `assets/logo/` folder are preserved for historical compatibility. The app uses the two selections above for its current dark header and favicon.